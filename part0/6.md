```mermaid
flowchart TD
    START2([Add note]) --> BTN[Save button press] --> BLOCK[Get form data]
    BLOCK --> BLOCK2[Show added note on the page]
    BLOCK2 --> BLOCK3[Send data to server] 

    BLOCK3 --> |POST_https\://studies.cs.helsinki.fi/exampleapp/new_note_spa| END([Server])